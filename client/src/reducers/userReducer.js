const initialState = {
    currentUser: {},
    isAuth: false,

}

export default function(state = initialState, {type, payload}) {
    switch(type) {


        default:
            return state
    }
}